root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -buildvcs=false -o ./tmp/main ."
  bin = "./tmp/main"
  exclude_dir = ["tmp", "vendor"]
  include_ext = ["go"]

[screen]
  clear_on_rebuild = true

[log]
  time = true
